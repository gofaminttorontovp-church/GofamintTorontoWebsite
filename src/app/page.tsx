import Hero from "@/components/Hero";

/**
 * Home page — just the scroll-driven hero (the "first section"). The About /
 * Visit / Sermons / Events content lives on its own routes under the (site)
 * route group.
 */
export default function Home() {
  return <Hero />;
}
